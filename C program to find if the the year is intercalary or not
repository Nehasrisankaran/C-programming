#include<stdio.h>
int main()
{
    int a;
    scanf("%d",&a);
    for(int i=0;i<a;i++)
    {
        int b;
        scanf("%d",&b);
        if(b%100!=0&&b%4==0||b%400==0)
        {
            printf("Yes\n");
        }
        else
        {
            printf("No\n");
        }
    }
}
