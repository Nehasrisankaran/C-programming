#include<stdio.h>
int main()
{
    int a;
    scanf("%d",&a);
    int ar[a];
    for(int i=0;i<a;i++)
    {
        scanf("%d",&ar[i]);
    }
    int c=0;
    for(int i=0;i<a;i++)
    {
        if(ar[i]>0)
        {
            c++;
        }
    }
        printf("%d",c);
        }
    
