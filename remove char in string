#include<stdio.h>
#include<string.h>
int main()
{
    char str[150];
    scanf("%[^\n]s",str);
    while(getchar()!='\n');
    char ch;
    scanf("%c",&ch);
    int l=0;
    for(int i=0;str[i]!='\0';i++)
    l++;
    for(int i=0;i<l;i++)
    {
        if(str[i]!=ch)
        {
            printf("%c",str[i]);
        }
    }

}
